---
type: state
title: "Known technical debt"
---

# Known technical debt

Recorded so it is not rediscovered as a surprise. **This file answers
one question: what is still wrong?**

**A resolved item is deleted from this file**, once `CHANGELOG.md`
records it. It is not marked "Closed" and left in place - the changelog
already answers *what was fixed*, with the date and the reasoning.
Closed entries left here had grown to roughly half the file, which is
how a page meant to be read before starting work becomes a page nobody
reads.

The exception is an item whose resolution changed what it is rather than
removing it. Those are rewritten, not deleted, and say what changed.

## How to read the impact column

Sorted within each table by impact, so the top of a table is where to
start.

| Grade | Means |
|---|---|
| **safety** | could leave hardware energised, or a sample protected by a limit nobody chose |
| **data** | could produce, preserve or lose a wrong number |
| **evidence** | a clean report means less than it says - "none observed", not "none" |
| **correctness** | the software does the wrong thing, visibly |
| **maintenance** | cost and churn, not risk |

## Instruments and transports

| Item | Impact | Evidence | Next action | Validation needed | Status |
|---|---|---|---|---|---|
| **Several drivers cannot be asked what range they are on.** The readback *contract* is no longer the gap - `core/readback.py` gives range, compliance and power limit a five-state answer, and a disagreement is a loud failure whether or not the readback itself is verified. What remains is per-driver query spellings. | evidence | Implemented on the 2611A and 2635B across all four axes (TSP attribute reads, the mechanism those drivers already use for `localnode.linefreq`), and on the GSM-20H10 for both measurement axes - where `SENS:CURR:DC:RANG?` is the query the bench read `1.050000E-05` from on 2026-08-20. See [fault 33](../faults/33-a-setting-never-read-back.md). | Confirm a query spelling at the bench for the 2401, 2450, B2901A and miniSMU, then implement. The U2722A's `SOUR:CURR:RANG?` is an open question in its own note rather than a fact. | Bench. **Guessing is not the conservative option**: an unrecognised *command* is logged and ignored, but an unrecognised *query* is never answered, times out, and latches the transport. A guess costs a run rather than a line in a report. | Open on four models, recorded as `unsupported` in the contract ledger with the reason |
| **No range readback anywhere is verified.** `RANGE_READBACK_TRUSTED` is False on every driver, so an agreement reports `unverified` - a warning, never a pass. | evidence | A *disagreement* is still a failure, which is what closed the GSM-20H10 case above. | One bench step per instrument. | Set a range from the front panel, ask for it over the bus, confirm the answer names the range that is physically selected. | Open, fleet-wide |
| **Most compliance readbacks are unimplemented.** So a clean checkup on those models means *none observed*, not *none*. | evidence | `compliance_readback` in the contract ledger records which. Only the GSM-20H10 (verified 2026-08-20) and the U2722A (verified 2026-08-24, including the case of a limit the instrument had refused) both implement it and have it checked. | Implement per driver. | One bench session each: set a distinctive compliance, read it back, confirm it agrees. | Open on the remaining six of the fleet <!-- lint-ok --> |
| **A sub-count source level is unguarded everywhere except the U2722A.** Below one count of the active range, is the output residue with uncommanded sign, as it is there? Only that instrument refuses it. | safety | Deviation 54 refuses a level below ten counts of the active range on the U2722A, after the bench established that `-1 µA` and `+1 µA` produced the same output on R120mA, and that during the commissioning round the residue pointed the wrong way and drove the output to the range rail. Nothing about that mechanism is specific to the U2722A - any fixed-range converter has a bottom count. | It is now *declared* rather than absent: every driver carries `SUB_COUNT_LEVELS` per axis with one of `refused`, `unmeasured`, `not applicable`, the contract ledger forces the decision, and the checkup raises a **warning** on every `unmeasured` axis. Close each with a measurement, not an edit. | Command plus and minus a small fraction of a count on a wide range, and see whether the output follows the sign. | Open and unmeasured on the Keithleys, the B2901A and the GSM-20H10. The refusal mechanism is in `BaseSMU` (`source_level_floor()`, `guard_source_level()`), so a driver that measures its converter declares a floor and gets the refusal. **No fleet-wide floor was invented**: a threshold on an instrument nobody has measured would refuse levels that may be perfectly good |
| **The miniSMU's two range flags may not describe the instrument.** `INDEPENDENT_SOURCE_RANGE = False` claims a source and measure range share a knob; on the miniSMU there is no source current range at all. | correctness | Established 2026-08-27 from the vendor library's wire commands: the voltage range sends `SOUR1:VOLT:RANGE`, the current range sends `CH1:IRANGE`, and `set_autorange` switches range "for the measured current". Its current range is a **measurement** range. | Leave alone for now. Changing the flags changes `apply_ranges()` on a working driver and needs its own wave. | The resulting behaviour is correct today and the driver is commissioned against it. Also unmeasured: what a sub-count source level even means on an instrument with no source current range. Its current axis is declared `not applicable`, its voltage axis `unmeasured` like the rest | Open, deliberately deferred |
| **The GSM-20H10's intermittent USB-TMC read timeout is not explained.** | data | Three runs lost 2026-08-25, six consecutive 2026-08-27, none in four on 2026-08-28 - same backend, same code, no configuration change. It predates Wave 8a: the 2026-08-25 bench notes record it, and before the latch it cost a run rather than stopping one. Excluded **by probe, not by argument**: the command is implemented, it answers on an empty queue, `*RST` is not still executing, no single command in the reset block is guilty, and the whole session replays over the console at machine speed without failing. | Two changes would make the next occurrence informative rather than a repeat. **One bounded read after the timeout, before latching**, recording whether the reply arrives late, never arrives, or is something else - evidence, not recovery, and it would have answered this in one run. **A verified resync** to replace the blanket refusal. | Wave 8a removed the device clear on evidence from `libusb-win32`, where it returned False, and that was never measured on USB-TMC. A clear followed by a discriminating query, continuing only on proof, keeps the guarantee without generalising one backend's measurement to the fleet. | Open |
| **A requested timeout was not the timeout in force.** From the same traces: the driver asks for `timeout_s=3.0` and the failure took 4.01 s every time. | correctness | `VisaTransport._read()` sets `res.timeout = 3000` and restores it, so something else determined the interval. | Find what actually bounds the read. | A timeout the caller cannot set is a timeout the caller cannot reason about. | Open |
| **`limitp` on the 2635B is watched, and the watching is unverified.** | evidence | `read_power_limit()` sends `print(smua.source.limitp)` and the checkup compares it against the 0 the driver writes. A nonzero value is a loud failure, because a power ceiling overrides whichever of the three limits is lower and `limitv` reports the programmed value rather than the effective one. | The same one bench step as every other readback here. | `POWER_LIMIT_READBACK_TRUSTED` is False, so an agreement reports `unverified` rather than `pass`. This instrument has never been on a bench at all, so the flag cannot move until it has. | Open |
| **`:TRACe:FEED?` is never asked.** The driver probes three tokens and caches whichever the instrument accepts, which works. | evidence | A buffer left on `CALCulate1` returns math results where raw readings are expected, and nothing in the data says so. Same readback lesson as D5/D6. | One query replaces three writes and three drains, leaves no `-140`s in the queue for the next reader to misdiagnose, and - the actual reason - says what the buffer is *storing*. | Bench confirmation that the query is answered. | Open |
| **Direct GPIB-HS is commissioned but not stress-tested.** The B2901A passed all three tiers on 2026-08-18, which covers ordinary use. | evidence | Described in [Direct NI GPIB-USB-HS transport](../architecture/direct-gpib-usb-hs.md). | Four narrower questions have never been put to hardware: a deliberately large sweep reply against the upstream synchronous read limit and its truncation boundary; a deliberately induced timeout; repeated connect/disconnect, including after a failed connection; and whether `GPIB0::<addr>::INSTR` locks out a simultaneous VISA claimant through the shared ownership key. | On the induced timeout: since Wave 8a the answer does not change what the software does - a read timeout latches the transport either way, and `clear()` no longer starts a session - but it decides whether that latch is conservative or necessary here. | Open |
| **The checkup passes on the U2722A, and the note still records the old failure.** *(Was: "can now pass, and has not yet been run on it".)* | evidence | It has now been run: the 2026-09-04 round returned 66 pass, 0 warn, 0 fail, 10 skip at commit `727022f`, with the current axis probed at 73.2 µA - ten counts of R120mA - instead of a seventh of one count. [Fault 34](../faults/34-a-probe-the-instrument-cannot-express.md) is confirmed closed against hardware. | Re-run once the driver readback work lands, and copy `last_bench`, `bench_code` and `bench_result` from *that* report's header. | The 2026-09-04 round ran at `727022f`, and `7d86900` has since changed `drivers/base_smu.py` - which every driver's fingerprint covers - so it no longer describes the running code and cannot be used to set the frontmatter. `bench_result: fail` from 2026-08-25 therefore still stands in the note, and is now known to be out of date rather than merely old. | Open, narrowed: the question is answered, the record is not updated |
| **`verify_compliance()`'s unsupported message says "compliance" where it means the limit value.** `drivers/base_smu.py` builds `unsupported_detail` as "`<model>` does not report its compliance", which is true of the limit readback and false of the trip flag - three drivers report the flag correctly. | evidence | [Fault 46](../faults/46-one-message-for-two-different-gaps.md). The checkup no longer renders that string - `Checkup._compliance_blindness()` asks the driver which gap it is looking at and words the skip itself - so nothing today shows the ambiguous sentence to an operator. | Narrow the wording at its source, in the same pass that adds the limit readbacks. | None. It is a string, and the correct reading is already established. | Open in `drivers/`, not rendered anywhere |
| **The miniSMU's traffic bypasses the transport contract.** Its driver calls `transport.client.<method>()` directly, so the desync latch in `Transport.query()` never sees it. | correctness | `tests/test_transport_desync.py` exempts it by name. | Ask `minismu_py` whether its client can desynchronise in the same way. | `MiniSMUTransport` carries method calls rather than a text request/response stream, so it probably cannot - **but that is a suspicion, not a measurement**, and nobody has asked. | Open |
| **The stage's shutdown confirmation names the dangerous states, not the safe ones.** `confirm_pid_off()` treats `HEATING` and `COOLING` as driving and everything else as not, so a firmware that grew a third driving state would read as confirmed off. | safety | The inverse default was rejected deliberately - every unrecognised state would then warn on every close, and a warning that always fires is one nobody reads. | Compare the firmware's own status enum against this set at connect. One line. | Until then the pairing is a claim about the firmware that nothing checks. | Open |

## Provenance and derived files

| Item | Impact | Evidence | Next action | Validation needed | Status |
|---|---|---|---|---|---|
| **`code_fingerprint()` hashes the path string**, so an absolute path makes the digest machine-specific. | data | Found 2026-08-28 by calling `code_paths_for()` with an absolute source path and spending twenty minutes hunting a divergence that was not there. The same driver spelled with a backslash separator and with a forward slash currently hashes to two different digests. | Normalise to repo-relative POSIX form inside the digest; the separator question goes away with it. | The path is included deliberately - without it, drivers with identical contents would be indistinguishable. `tools/smu_checkup.py` passes relative paths, so no shipped report is affected. The symptom would be an instrument reading stale on another machine with nothing actually different. | Open |
| **`SampleRegistry` is process-local**, like the ownership manager. Two instances of the app would each hold their own label-to-identifier map, so the *same* physical sample measured in both would be minted twice and the carry-over between experiments would not be provable. | data | - | Revisit alongside the ownership manager if the answer to "can two instances run at once?" turns out to be yes. | Both need revisiting together. | **Narrowed, not closed.** The live hazard - the registry's collision re-draw being the only thing between two processes and a shared identifier - is gone. The random tail is 64 bits, so uniqueness across processes rests on the width rather than on a check that only ever saw one process's identifiers, and run identifiers carry a per-process `SESSION_ID` |
| **The operational log transcribes `metadata` verbatim**, by design - an operator note belongs in the log. | data | `experiments/base_experiment.py` passes `context.metadata`, which today holds parameters and notes and never readings. | Worth a narrowing rule - an allowed key list, or a numeric-value refusal - if a future experiment starts putting richer things in metadata. | Nothing in `core/event_log.py` can stop a caller putting a measurement value in it, so the operational-log boundary holds there by convention rather than by construction. | Open, low |
| **The generated indices record `file:line`.** `deviation-index.md` cites source line numbers, so *any* edit that shifts a line makes it stale and fails `test_generated_pages_match_a_fresh_build`. | maintenance | - | `uv run python tools/build_docs.py` belongs in every patch that touches code, not only in documentation patches. | The scan is over tracked files only ([fault 35](../faults/35-derived-from-whatever-is-lying-around.md)), which removes the untracked-junk half and adds a smaller consequence: a **new** module contributes no row until it is `git add`-ed. Deliberate - these pages are committed artifacts - but it reads as a missing row to somebody who has not staged their work. | Open, accepted |
| **A failing generated-page test leaves the working tree dirty.** That test writes the fresh build before asserting, so an isolated re-run passes against the files the failed run just regenerated. | maintenance | It looks like order-dependence and is not. | Restore on failure. | The second run would then tell the truth. | Open |

## The test suite

| Item | Impact | Evidence | Next action | Validation needed | Status |
|---|---|---|---|---|---|
| **GUI test files patch `messagebox` on shared modules at import time**, so in one pytest process the last file imported wins and the others assert against a recorder nothing writes to. | correctness | Measured in [the test suite audit](../reference/test-suite-audit.md). `run_tests.py` hides it by giving each GUI file its own process. | A fixture that patches and restores per test, the way `_no_instrument_discovery` already does in `conftest.py`. | A shared-layer change across every GUI file, so it wants a wave of its own and must not ride along with an unverified instrument. It buys no speed; it is a correctness change. | Open, deferred deliberately - the hazard only fires under plain `pytest`, which is not how this suite is run |
| **A guard for the above exists; the fix does not.** `_dialog_recorder_belongs_to_this_file` in `tests/conftest.py` fails any GUI test whose dialog seam is owned by another test file, so a hand-run `pytest tests/` reports the contamination instead of a meaningless green. | correctness | It checks ownership by identity at the moment a GUI test runs. | Per-test patch-and-restore in each file, as above. | Two files that install their recorder inside a fixture rather than at import are not covered, and do not need to be. | Guard in place, fix open |
| **A seam nobody claimed is a separate hazard, also guarded.** `_a_gui_test_never_reaches_a_real_dialog` fails a GUI test that raises a dialog on a seam no test has stubbed - shown or merely left in the UI queue. | correctness | Ownership cannot catch that case: with no owner there is nothing to disagree with. See [fault 28](../faults/28-a-dialog-nobody-stubbed.md). | Per-test patch-and-restore, which would make the question moot. | - | Guard in place, fix open |
| **A stateful fake gives a different answer to a second `Checkup`.** A fake that consumes state as it is read - a ramp, a queue, a one-shot fault - has already spent it by the time a second run starts, so an assertion made against run two can pass while the behaviour under test is broken. | correctness | Recorded because it cost a mutation round. | Take every result from one run. | Not fixed, and probably not fixable in general. | Open, worked around |
| **Order-dependent test files.** Eight files share Tk roots and fake-driver classes across tests via `global`, preserving the behaviour of the original scripts. They cannot be run in isolation and would break under `pytest-xdist`. | maintenance | - | Convert to module-scoped fixtures as each experiment is touched. | - | Open |
| **Two test styles coexist**: converted section tests using the `check` fixture, and wrapped collector tests. | maintenance | Documented in `tests/README.md`. | - | - | Open, accepted |
| **The Keithley 2450 has no dedicated driver test file**, alone among the text-dialect drivers, and is covered only by the registry-driven contract files. | maintenance | A mutation confirmed the practical effect: changing one of its output spellings turned exactly one generic test red. | Belongs to whichever wave next touches that driver. | Not urgent, and not obviously wrong given the contract files - but it should be a decision rather than an accident. | Open |
| **`test_4pp.py` still drives `_do_run()` on the main thread.** Its green says nothing about threading; that is `test_4pp_lifecycle.py`'s job. | maintenance | - | Leave as-is. | Churning a 434-line passing test file is where a real regression hides. | Open, deliberate |
| **Coverage is not measured.** Review A-09 asks for coverage tracked by risk area - failure paths for close and shutdown, generated docs, driver readbacks - rather than a global percentage. | maintenance | Wave A added substantial tests in exactly those areas. Nothing reports on it. | The obstacle is `run_tests.py`: coverage over thirty separate pytest processes needs `coverage combine` and a parallel-mode configuration, which is a change to the runner and wants its own wave. | Enforcing a global threshold remains explicitly out of scope. | Open |

## The application

| Item | Impact | Evidence | Next action | Validation needed | Status |
|---|---|---|---|---|---|
| **Cancellation cannot preempt a reading in progress.** The settle delay is handed to the instrument with `set_source_delay()`, so it happens inside the driver's blocking `measure()`. | correctness | The honest bound is one reading, and `test_4pp_lifecycle.py` measures the rest of the path rather than asserting it. | Moving the settle into `run.sleep()` would make cancellation near-instant. | It would change *where the settle happens*, which is a measurement parameter and not a UI detail. Needs a bench comparison before it goes near real hardware. | Open |
| **Five `int(float(...))` call sites remain in the experiments** - 4PP points and reversals, IV points, runs and cycles. | correctness | Wave 2 built the validators that replace them but deliberately did not touch an experiment file. | Swap them over as each experiment is wired up. | The seven in `drivers/` parse SCPI error codes and are correct as they are - see [house rule 6](../rules/06-validate-operator-input.md). | Open |
| **`core.driver_registry`** remains as a deprecation shim re-exporting `drivers.registry`. | maintenance | `tests/test_packaging.py` asserts nothing *inside* the repo imports it. | Remove once nothing external imports it. | - | Open |

## Lint rules deliberately left off, with their counts

Review A-09 configured the first automated quality gate this project has
had. The criterion for a rule being in `pyproject.toml`'s `select` is
not "is it a good idea" but **"is it green on this tree today"**,
because only a green gate can be required to stay green - a run with
every rule enabled produces about 12,800 findings here, and a job that
starts 12,800 in the red is how a real failure gets waved through.

Each row is a scoped task rather than a rediscovery: adopting one means
fixing its findings and deleting its line from `ignore`.

| Rule | Count | Why it is off | What adopting it takes |
|---|---|---|---|
| `B905` | 23 | The most valuable of these, and the one that most needs a person. A silently truncated zip of readings against labels is exactly the class of fault this project keeps finding. | A per-site judgement, and getting it wrong is not neutral: `strict=True` on a pair that legitimately differs turns a working measurement into a crash at the bench, and `strict=False` everywhere is a rubber stamp that makes the rule permanently useless. It wants a pass with the data in front of you, one call site at a time. |
| `S110`, `S112` | 74, 3 | Not deferred for effort - **a linter cannot answer the question.** A suppression in a `close()` path is correct and one on a data-preservation path is [fault 29](../faults/29-a-shutdown-that-fails-open.md), and the two are spelled identically. | Nothing. [House rule 13](../rules/13-exceptions-are-not-suppressed-silently.md) states the policy and `tests/test_exception_policy.py` enforces the half that can be enforced mechanically. |
| `BLE001` | 209 | The superset of the above and the same argument, with the difference that most of these handlers *do* something. | Worth revisiting only after the guarded surface below is complete. |
| `E402` | 416 | Almost all are the deliberate `pytestmark = [pytest.mark.gui]` before the imports, and imports after a `sys.path` insert in the tools. | A `per-file-ignores` entry per file, which buys a rule that would then be ignored in every file it fires in. |
| `RUF100` | 35 | It fires on every `# noqa: E402` in the tree, and those are correct - they mark an import that deliberately follows a path insert, and are load-bearing documentation whether or not `E402` is on. Deleting 35 of them would delete the explanation rather than the staleness. | Worth enabling the day `E402` and `BLE001` are. |
| `E741` | 54 | An ambiguous variable name (`l`, `I`, `O`). | Renaming 54 variables across the drivers and the maths modules is real churn on files whose diffs are read closely; it belongs to whichever wave next touches each file. |
| `DTZ005` | 19 | `datetime.now()` with no timezone. These write local naive timestamps into CSV headers and file names. | Changing them changes the stored data format, which is a schema decision and not a lint fix. |
| `ANN`, pylint C/R/W | 6,140 and 816 | The noise the audit measured. `PLE` is 0 and is enabled. | Type annotations are being adopted at the boundaries instead - see `[tool.mypy]` in `pyproject.toml`. The complexity findings are a design conversation rather than a gate. |

**The exception policy covers a named surface, not the tree.** 55 of the
62 blind suppressions in production code had no stated reason when house
rule 13 was written. The fourteen files in
`tests/test_exception_policy.py`'s `GUARDED` list were given one; the
drivers, transports, panels and tools were not. Doing all 55 in one
change would produce 55 sentences nobody had time to mean, so it is a
per-area pass, and adding a file to `GUARDED` is how each area is
closed.
